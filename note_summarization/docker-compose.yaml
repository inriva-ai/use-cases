version: "3.9"

services:
  web:
    build: .
    env_file:
      - .env
    container_name: ns-fastapi-app
    ports:
      - "8000:8000"
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    volumes:
      - .:/app
    restart: unless-stopped